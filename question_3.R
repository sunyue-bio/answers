#I tried to solve this problem using r language programming, but I did not achieve unity in the data format.
#Below is the method I used.

#Fitting Multi Layer Perceptron
mlp.fit <- mlp(y.in)
plot(mlp.fit)
print(mlp.fit)